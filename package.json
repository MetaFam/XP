{
  "name": "template-instance",
  "version": "0.1.0",
  "description": "MetaGame's XP System powered by SourceCred",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "sourcecred": "0.7.0-beta-16"
  },
  "scripts": {
    "clean": "rimraf cache site",
    "clean-all": "yarn clean && rimraf output",
    "load": "sourcecred load",
    "graph": "sourcecred graph",
    "score": "sourcecred score",
    "site": "sourcecred site",
    "go": "yarn graph && yarn score && yarn site && yarn serve",
    "serve": "sourcecred serve",
    "grain": "sourcecred grain"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
